{
  "name": "pasta",
  "version": "1.0.0",
  "description": "Chrome extension that intelligently extracts and copies the main content of webpages to clipboard",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "keywords": [
    "chrome-extension",
    "clipboard",
    "content-extraction"
  ]
}
